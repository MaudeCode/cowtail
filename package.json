{
  "name": "cowtail-workspace",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "web",
    "protocol"
  ],
  "scripts": {
    "build:cli": "cd cli && bun run build",
    "codegen:ios": "cd protocol && bun run codegen:ios",
    "dev:web": "cd web && bun run dev",
    "check:cli": "cd cli && bun run check",
    "build:web": "cd web && bun run build",
    "check:protocol": "cd protocol && bun run check",
    "lint:ts": "oxlint web/src web/convex web/vite.config.ts cli/src cli/tests protocol/src",
    "lint:ts:fix": "oxlint --fix web/src web/convex web/vite.config.ts cli/src cli/tests protocol/src",
    "fmt:ts": "oxfmt --write web/src web/convex web/vite.config.ts cli/src cli/tests protocol/src",
    "fmt:ts:check": "oxfmt --check web/src web/convex web/vite.config.ts cli/src cli/tests protocol/src"
  },
  "devDependencies": {
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0"
  }
}
